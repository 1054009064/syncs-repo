name: Sync Danmu_api repository

on:
  push:
    paths:
      - '.github/workflows/sync-danmu.yml'
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  danmu:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout git repository
        uses: actions/checkout@main
        with:
          repository: 1054009064/danmu_api
          token: ${{ secrets.CI_LINUX }}
          fetch-depth: 0

      - name: Configure git
        run: |
          git --version
          git config --global user.email "${{ secrets.GIT_EMAIL }}"
          git config --global user.name "${{ secrets.GIT_USERNAME }}"

      - name: Add remotes repository
        run: ${{ secrets.DANMU_ARR }}

      - name: Force sync branches
        run: ${{ secrets.DANMU_FSB }}

      - name: Last check space usage
        run: df -hT
